import type { Plugin } from 'vite';
import SVGO from 'svgo';
export default function (options?: {
    defaultImport: string;
    svgoConfig?: SVGO.OptimizeOptions;
}): Plugin;
export interface AbstractNode {
    tag: string;
    attrs: {
        [key: string]: string;
    };
    children?: AbstractNode[];
}
