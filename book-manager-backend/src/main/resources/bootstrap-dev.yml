server:
  port: 8100

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_manager?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8
    username: root
    password: Java213213
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl # mybatis-plus日志打印
    map-underscore-to-camel-case: true # 开启实体类与数据库字段的驼峰映射
  mapper-locations: classpath*:mapper/*.xml # 扫描mapper.xml文件
  typeEnumsPackage: my.**.enums # 枚举与code值转换